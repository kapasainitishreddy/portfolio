"use client";

// Theme interactions now happen directly on the background surface. Keeping
// this component preserves the layout API without adding floating UI chrome.
export default function ThemeExtras(){return null;}
