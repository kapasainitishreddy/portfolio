import type { Project } from "@/data/projects";

/**
 * Reserved bucket for future highlighted work. Currently unused — the resume
 * leads with Forward Deployed Engineer case studies in `projects.ts`.
 */
export const publicGoodProjects: Project[] = [];
