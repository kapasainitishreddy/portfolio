name: Netlify Deployment Note

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Portfolio is deployed to Netlify. Use the Netlify project dashboard for future deployments."
